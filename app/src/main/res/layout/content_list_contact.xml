<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/colorWhite"
    android:layout_height="match_parent"
    tools:showIn="@layout/app_bar_home">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view_contact_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="@dimen/recycler_padding_bottom"
        android:scrollbars="vertical">

    </android.support.v7.widget.RecyclerView>




    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_add_contact"
        android:layout_width="wrap_content"
        android:backgroundTint="@color/colorPrimary"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="@dimen/padding_padrao"
        android:src="@drawable/ic_add"
        app:fabSize="normal"
        app:layout_behavior="systemplus.com.br.listadecontatos.animate.FloatingActionButtonAnimate" />

    <LinearLayout
        android:id="@+id/added_contact"
        android:layout_width="match_parent"
        android:gravity="center"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/colorPrimaryDark">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/padding_padrao"
            android:paddingTop="@dimen/padding_padrao"
            style="@style/AppTheme"
            android:layout_gravity="center"
            android:textColor="@color/colorWhite"
            android:text="@string/contact_added" />
    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>